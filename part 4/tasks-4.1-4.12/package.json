{
  "name": "blog-list",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "Serhii Didenko",
  "type": "module",
  "main": "index.mjs",
  "scripts": {
    "start": "cross-env NODE_ENV=production node index.mjs --env-file=.env",
    "dev": "cross-env NODE_ENV=test node --watch --env-file=.env index.mjs",
    "test": "cross-env NODE_ENV=test node --env-file=.env --test --test-only"
  },
  "dependencies": {
    "@js-temporal/polyfill": "^0.5.1",
    "cross-env": "^10.1.0",
    "express": "^5.2.1",
    "mongoose": "^9.1.1"
  },
  "devDependencies": {
    "supertest": "^7.1.4"
  }
}
